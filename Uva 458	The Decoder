#include<stdio.h>
#include<string.h>

int main()
{
    char s[1000];
    int i;
    while(gets(s))
    {
        for(i=0;i<strlen(s);i++)
        {
            printf("%c",s[i]-7);
        }
        printf("\n");
    }
}
