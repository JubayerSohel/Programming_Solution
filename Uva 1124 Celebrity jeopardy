#include<stdio.h>
#include<string.h>
int main()
{
    char c[100];
    int i,l;
    while(gets(c))
    {
        l=strlen(c);
        for(i=0;i<l;i++)
        {
        printf("%c",c[i]);
        }
        printf("\n");
    }
    return 0;
}
