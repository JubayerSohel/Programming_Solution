#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,l,i;
    string s;
    cin>>t;
    while(t--)
    {
        cin>>s;
        sort(s.begin(),s.end());
        l=s.size();
        for(i=l-1;i>l-2;i--)
        {
            cout<<s[i]<<endl;
        }
    }
}

